name:  {{ cookiecutter.env_name }} # name of the environment

## environment dependencies for development
## keep runtime dependencies in pyproject.toml
dependencies:
  - python={{ cookiecutter.python_version }} # set python version
  - pip # install package manager

  - pip:    
    - build  # to allow build WHL pachages
    - ipykernel # to load env as kernel
    - IPython  # to load proper ipython extensions, 9.0 is broken
    - nbdime # to allow GIT to work with jupyter notebooks, integrate with git using `nbdime config-git --enable --global`
    - pytest # for running python test
    - pytest-cov # for running python test coverage report
    - toml   # for working with .toml files

# EOF
